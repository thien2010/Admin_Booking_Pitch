<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.booking_pitch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_png"
        android:label="Quản lý sân bóng"
        android:roundIcon="@drawable/logo_deciption_png"
        android:supportsRtl="true"
        android:theme="@style/Theme.Booking_Pitch"
        android:usesCleartextTraffic="true">
        <activity android:name=".Admin.NewsActivity"></activity>
        <activity android:name=".Admin.PitchActivity" />
        <activity android:name=".Admin.AdminActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>